{% extends "::base.html.twig" %}

{% block title %}MOTOPrincipalBundle:Login:Login{% endblock %}

{% block body %}
    <div class="container">
        <form action ="{{path('_login')}}" method="post">

            {{form_widget(form)}}
            <input type="submit" value="Iniciar sesion"/>
        </form>
        {% if error != '-' %}
            <div class="alert alert-danger" role="alert">
                <p>{{ error }}</p>
            </div>
        {% endif %}
    </div>


{% endblock %}
